# 00919 監控儀表板 SOP

最後更新：2026-05-25

## 1. 專案目的

建立一套 00919 高股息 ETF 監控儀表板，讓使用者不用每天盯盤，也能用固定節奏確認：

- 價格與淨值是否偏離。
- 自己的含息報酬是否仍為正。
- 每季配息與 54C 稅務估算是否需要注意。
- ETF 規模、受益人數與前十大持股是否出現異常。
- 未來手機端可以用首頁快速判斷，電腦端保留完整明細。

核心原則：**首頁看結論，子頁查原因。**

## 0. 目前接續狀態與換電腦快速提醒

最後接續狀態：2026-05-25，手機版首頁已驗收 OK，電腦版首頁第一版改版已完成。手機版首頁維持獨立 DOM：`section.mobile-home`；電腦版首頁新增獨立 DOM：`section.desktop-home`。電腦版首頁已完成新版 sidebar、青綠藍 Hero Banner、Hero 右側目前股數 / 含息報酬摘要卡、核心數據九格、首頁重點六卡、走勢總覽大圖、五張模組導覽卡與小型資料更新狀態欄。今晚已處理 GitHub Desktop pull merge conflict，保留桌機首頁 v1 版本；明天到公司電腦先完成 merge / push，再看雲端畫面。下一步是把同一套桌機 UI 標準延伸到每月、每季、持股、每年、燈號設定等分頁。

目前最新可上傳封包：

- `00919_streamlit_cloud_package_20260525_desktop_home_v1_ready.zip`

若換電腦或隔天繼續，優先照這個順序確認：

1. 本機版是否可開：雙擊 `啟動00919儀表板.bat`，確認 `http://localhost:8787/index.html` 正常。
2. 雲端版是否可開：登入 Streamlit Cloud dashboard，開啟 `00919-dashboard-streamlit` app。
3. 若雲端畫面不是本機 HTML UI，或桌機首頁 / 手機首頁不是新版設計，重新上傳最新封包 `00919_streamlit_cloud_package_20260525_desktop_home_v1_ready.zip`。
4. 上傳方式：解壓 ZIP，把裡面所有檔案覆蓋到 GitHub Desktop clone 下來的 repo 最上層。
5. GitHub Desktop commit 後按 `Push origin`。
6. Streamlit Cloud 等自動部署，若沒有自動部署就進 `Manage app` 重啟或重新部署。

公司電腦接續建議流程：

1. 先用 GitHub Desktop 開啟 repo `00919-dashboard-streamlit`。
2. 如果 GitHub Desktop 還顯示 merge 狀態，先確認沒有 conflict，再按 `Continue merge`。
3. 若已經完成 merge，按 `Push origin`，讓 GitHub / Streamlit Cloud 拿到最新版。
4. 若公司電腦還不是最新版，先按 `Fetch origin` / `Pull origin`。
5. 若要用本次封包接續，解壓 `00919_streamlit_cloud_package_20260525_desktop_home_v1_ready.zip`。
6. 將解壓後的所有檔案覆蓋到 GitHub Desktop clone 的 repo 最上層。
7. 確認重要檔案有覆蓋到：
   - `streamlit_app.py`
   - `fetch_data.py`
   - `index.html`
   - `assets/app.js`
   - `assets/styles.css`
   - `static/index.html`
   - `static/assets/app.js`
   - `static/assets/styles.css`
   - `00919手機監控_出版SOP.md`
8. GitHub Desktop 左下 `Summary` 可填 `add desktop home redesign` 或 `update desktop home package`。
9. 按 `Commit to main`，再按 `Push origin`。
10. Streamlit Cloud 部署完成後，用桌機、手機直向、手機橫向各看一次。

今晚已處理過的 merge conflict：

- `00919手機監控_出版SOP.md`
- `static/assets/app.js`
- 處理原則：保留 `desktop_home_v1`、`section.desktop-home`、`renderDesktopHome(...)`、`drawDesktopTrendChart(...)` 等桌機首頁新版內容。
- 若 GitHub Desktop 又跳出 conflict，優先保留桌機首頁 v1，不要退回 `mobile_spacing_v4`。

一般覆蓋封包流程：

1. 解壓最新封包。
2. 將解壓後的所有檔案覆蓋到 GitHub Desktop clone 的 repo 最上層。
3. 確認重要檔案有覆蓋到：
   - `streamlit_app.py`
   - `fetch_data.py`
   - `index.html`
   - `assets/app.js`
   - `assets/styles.css`
   - `static/index.html`
   - `static/assets/app.js`
   - `static/assets/styles.css`
   - `00919手機監控_出版SOP.md`
4. GitHub Desktop 左下 `Summary` 填本次修改摘要。
5. 按 `Commit to main`，再按 `Push origin`。
6. Streamlit Cloud 部署完成後，用桌機、手機直向與橫向各看一次。

GitHub Desktop 的 `Summary` 不是固定值，只是本次修改的簡短標題。常用範例：

- 修手機畫面：`fix mobile layout`
- 修手機滑動：`fix mobile scroll issue`
- 修雲端轉圈：`fix streamlit cloud loading`
- 恢復原本 HTML UI：`restore html ui`
- 更新 SOP：`update sop`

目前 GitHub repo 名稱：

- `00919-dashboard-streamlit`

目前雲端架構重點：

- 桌機版保留原本 HTML UI 與完整分頁。
- 手機版以寬度 `760px` 以下自動判斷，只顯示新版獨立手機首頁 `section.mobile-home`。
- 判斷依據是瀏覽器寬度、低高度橫向視窗與觸控媒體條件，不是 GPS、城市或登入裝置名稱；手機橫放也應顯示手機版。
- Streamlit Cloud 版用 `streamlit_app.py` 內嵌 HTML/CSS/JS，避免直接 iframe 載 `static/index.html` 造成轉圈。
- 手機滑動修正版已避免內外兩層 scrollbar 搶觸控。
- 手機版暫時不顯示底部固定導覽列；分頁切換導覽先保留給後續桌機版/完整分頁規劃。
- 手機版首頁目前視為 OK；後續若不是 bug，不要優先調整手機首頁。
- 電腦版首頁目前已有第一版新版 UI；下一階段重點是把同一套金融 App 視覺語言延伸到其他桌機分頁，但不要改資料抓取、交易紀錄、配息、稅務計算邏輯。
- 雲端執行環境時間可能是 UTC，`fetch_data.py` 必須用 `Asia/Taipei` 寫入 `fetched_at`，避免台灣早上更新卻顯示前一天日期。
- 價格、淨值、市值、未實現損益、含息損益這類每日資料的更新狀態，要用交易日判斷，不要只看日曆天數；遇到週末後的週一，5/22 到 5/25 不應直接顯示資料異常。

## 2. 目前系統架構

### 2.1 本機版

- 開啟方式：雙擊 `啟動00919儀表板.bat`
- 本機網址：`http://localhost:8787/index.html`
- 本機伺服器：`dashboard_server.py`
- 資料抓取：`fetch_data.py`
- 畫面來源：`index.html` + `assets/*.js` + `assets/styles.css`
- 主要資料檔：
  - `data/dashboard_data.json`
  - `data/trades.json`
  - `data/monthly_history.json`
  - `data/holdings_history.json`

批次檔流程：

1. 切到專案資料夾。
2. 執行 `python fetch_data.py` 更新資料。
3. 檢查 `http://localhost:8787/api/ping`，確認新版本機伺服器是否已啟動。
4. 若伺服器未啟動，執行 `python dashboard_server.py`。
5. 開啟 `http://localhost:8787/index.html`。

### 2.2 Streamlit Cloud 版

- 雲端入口：`streamlit_app.py`
- 雲端平台：Streamlit Cloud + GitHub
- 雲端畫面目標：保留本機 `.bat` 版的 HTML UI，也就是與 `index.html` / `assets` 顯示效果一致。
- 雲端資料來源：repo 內的 `data/*.json`
- 目前最新雲端封包：`00919_streamlit_cloud_package_20260525_desktop_home_v1_ready.zip`

注意：Streamlit Cloud 不適合直接用 iframe 載入 `static/index.html` 再執行 `assets/*.js` / `assets/*.css`。目前雲端 UI 修正版改由 `streamlit_app.py` 讀取 `static/index.html`、`assets/styles.css` 與各 JS 檔，內嵌到 Streamlit 元件中，避免雲端靜態檔副檔名與 MIME type 限制造成一直轉圈。

### 2.3 首頁更新按鈕

首頁「更新資料」按鈕的正式定位是全資料更新，不只更新價格。

更新範圍：

- 每日：市價、淨值、折溢價、成交量。
- 每月：AUM、受益人數。
- 每季：配息、54C、收益平準金、資本利得、補充保費估算。
- 持股：前十大持股、產業分布、持股汰換紀錄。
- 整體：更新時間、資料新鮮度、資料完整性。

目前本機版若由 `dashboard_server.py` 啟動，按鈕會呼叫 `/api/update` 執行 `fetch_data.py`。若仍使用舊的靜態伺服器，按鈕只能重新讀取現有 JSON，畫面會顯示靜態模式。

## 3. 頁面配置

### 3.1 首頁

首頁是未來手機版的主要畫面，資訊順序如下：

1. 倉位資訊：股數、平均成本、最早買進日、市值、投入本金、未實現損益、預估年配息、累積配息、含息損益。
2. 總燈號及股價資訊：總燈號、市價、淨值、折溢價、成交量。
3. 資料更新狀態：每日價格、每月規模、配息 / 54C、前十大持股、整體抓取、資料完整性。
4. 首頁重點總覽：含息報酬、最新配息、本次 54C、補充保費、AUM、受益人數、前十大集中度、第一大持股。
5. 每日圖表：市價、淨值、折溢價與成交量。

### 3.2 交易紀錄

用途：所有個人部位資料的來源。

手動輸入欄位：

- 買入 / 賣出。
- 交易日期。
- 交易股數。
- 買入 / 賣出價位。
- 資金來源 / 備註分類。
- 備註。

自動計算：

- 目前股數。
- 平均成本。
- 投入本金。
- 累積配息估算。
- 目前市值。
- 未實現損益。
- 含息損益估算。

匯入資料採新增合併，不覆蓋原本手動輸入資料。

### 3.3 每月健康檢查

用途：檢查 ETF 規模健康與自己的含息報酬。

重點：

- AUM。
- 受益人數。
- 月度含息報酬。
- 含息報酬折線圖。
- ETF 規模與受益人數圖表。

含息報酬每月都要計算，即使當月沒有交易，也要依持有股數、月底價格與累積配息更新。

### 3.4 每季配息與 54C

用途：檢查每季配息、配息組成與稅務估算。

重點：

- 每股配息。
- 除息日持股估算。
- 本次配息估算。
- 本次 54C 估算。
- 補充保費觀察。
- 每季配息組成圖。
- 歷史配息表。

54C 估算公式：

```text
每股 54C 估算 = 每股配息 * 股利所得占比
本次 54C 估算 = 每股 54C 估算 * 除息日持股股數
補充保費估算 = 本次 54C 估算 * 2.11%
```

注意：正式報稅仍以券商所得明細與報稅資料為準。

### 3.5 持股

用途：確認 00919 是否仍維持原本持股邏輯。

重點：

- 前十大持股占比。
- 產業分布。
- 第一大持股。
- 前十大集中度。
- 新增前十大。
- 跌出前十大。

目前不納入 ETF 重疊度，第一版以群益 / MoneyDJ 持股資料與前十大變化為主。

### 3.6 每年

用途：報稅季與年度總結。

重點：

- 年度配息總額。
- 年度 54C 估算。
- 54C 占比。
- 補充保費觀察。
- 年度燈號。

年度配息總額在四季資料都出來後才算完整年度。

### 3.7 燈號設定

用途：調整燈號門檻。

可調整項目：

- 折溢價黃燈 / 紅燈門檻。
- 月度含息報酬紅燈門檻。
- 受益人數連續下降月份。
- 單次 54C 門檻。
- 補充保費費率。
- 補充保費觀察金額。
- 前十大汰換門檻。
- 前十大集中度門檻。

設定存在目前瀏覽器，儲存後各頁燈號重新計算。

### 3.8 使用說明

用途：內建操作手冊。

內容包含：

- 專案目的。
- 首頁閱讀方式。
- 資料更新 SOP。
- 資料完整性警示。
- 燈號規則。
- 各分頁用途。
- 雲端版規劃。

## 4. 燈號與警示規則

### 4.1 總燈號

總燈號顯示在首頁。

- 綠燈：所有關鍵數據正常，文字顯示「所有數據正常」。
- 黃燈：有需要觀察的項目，但尚未嚴重影響核心計算。
- 紅燈：關鍵資料缺漏，或異常已影響市值、損益、含息報酬等核心計算。

總燈號會整合：

- 每日折溢價 / 成交量。
- 每月含息報酬與規模健康。
- 每年 54C 與補充保費。
- 持股集中度與汰換。
- 資料完整性。

### 4.2 資料完整性警示

目的：避免資料沒抓到卻被拿去計算。

檢查項目與影響：

- 市價缺漏：目前市值、未實現損益、含息報酬會失真，總燈號轉紅。
- 淨值缺漏：折溢價與淨值線會失真。
- 折溢價缺漏：每日燈號與折溢價警示會失真。
- 成交量缺漏：成交量觀察會缺資料。
- AUM 缺漏：ETF 規模健康會失真。
- 受益人數缺漏：受益人數趨勢與月度燈號會失真。
- 配息歷史缺漏：每季配息、年度配息與 54C 統計會缺資料。
- 54C 組成缺漏：54C 與補充保費估算會失真。
- 前十大持股缺漏：集中度、第一大持股與汰換紀錄會缺資料。

顯示位置：

- 首頁「總燈號及股價資訊」會顯示一句異常原因。
- 首頁「資料更新狀態」的「資料完整性」會顯示缺漏項目。

## 5. 資料更新 SOP

### 5.1 本機每日或不定期使用

1. 雙擊 `啟動00919儀表板.bat`。
2. 批次檔先執行 `fetch_data.py`。
3. 若資料抓取失敗，保留上次資料並顯示錯誤。
4. 啟動 `dashboard_server.py`。
5. 開啟 `http://localhost:8787/index.html`。
6. 首頁檢查總燈號與資料更新狀態。
7. 若需要補抓，按首頁「更新資料」。

### 5.2 更新後檢查

每次更新後先看首頁右上角：

- 每日價格是否正常。
- 每月規模是否正常。
- 配息 / 54C 是否正常。
- 前十大持股是否正常。
- 整體抓取是否完成。
- 資料完整性是否正常。

若任何一格變黃或紅，依總燈號文字提醒前往對應分頁檢查。

### 5.3 缺漏補抓原則

如果多天沒有開啟儀表板，下次更新時不應只抓當天資料，而要補抓缺漏期間：

- 日資料補齊缺漏交易日。
- 月資料保留累積歷史，不覆蓋舊月份。
- 配息資料以除息日作為唯一鍵，已有資料則更新欄位。
- 持股資料以資料日作為快照，供前十大汰換比較。

## 6. 雲端版 SOP 草案

雲端版目標：手機可看首頁，電腦可看完整頁面。

優先策略：

- 手動更新按鈕。
- 缺漏補抓。
- 資料新鮮度提醒。
- 不優先依賴排程，避免免費雲端額度被用光。

雲端需要替換的部分：

- 本機 `dashboard_server.py` 改為雲端後端或雲端函式。
- `fetch_data.py` 改為可在雲端執行的抓取服務。
- `data/*.json` 改為雲端儲存或雲端同步檔。
- 交易紀錄需改接雲端資料庫或雲端檔案，確保手機與電腦同步。

只要雲端提供同樣格式的 `dashboard_data.json` 與交易資料，前端燈號、圖表與資料完整性警示可以沿用。

### 6.1 雲端 UI 修正版上傳流程

當 Streamlit Cloud 已能開啟，但畫面不是本機 `.bat` 版的 UI，或頁面一直轉圈，改用 UI 修正版封包。

使用封包：

- 優先使用最新封包：`00919_streamlit_cloud_package_20260522_1800_mobile_scroll_fixed.zip`
- 舊版 UI 修復封包：`00919_streamlit_cloud_package_20260522_1800_ui_fixed.zip`
- 舊版手機版型封包：`00919_streamlit_cloud_package_20260522_1800_mobile_fixed.zip`

上傳步驟：

1. 開啟 GitHub Desktop。
2. 選擇 repo：`00919-dashboard-streamlit`。
3. 若尚未下載 repo，按 `Clone` 把 repo 下載到本機。
4. 解壓最新封包 `00919_streamlit_cloud_package_20260522_1800_mobile_scroll_fixed.zip`。
5. 把解壓後的所有檔案複製到 GitHub repo 的最上層。
6. 若 Windows 詢問是否取代檔案，選擇取代。
7. 確認 repo 最上層直接看得到：
   - `streamlit_app.py`
   - `requirements.txt`
   - `.streamlit/config.toml`
   - `data/`
   - `static/`
8. 回 GitHub Desktop，在 `Summary` 輸入 commit 訊息，例如 `restore html ui for streamlit cloud`。
9. 按 `Commit to main`。
10. 按 `Push origin` 上傳到 GitHub。
11. 回 Streamlit Cloud 等自動部署；若沒有自動部署，進 `Manage app` 後按重新部署或重啟。

### 6.2 雲端部署後測試流程

第一輪先測讀取已上傳 JSON，不要急著按更新資料。

1. 開啟 Streamlit Cloud app 網址。
2. 確認畫面不再一直轉圈。
3. 確認畫面風格接近本機 `.bat` 版，也就是左側選單、首頁卡片、圖表與各分頁都正常。
4. 首頁確認以下欄位有數字：
   - 市價
   - 淨值
   - 折溢價
   - AUM 規模
   - 受益人數
   - 前十大集中度
5. 點選左側分頁，確認：
   - `交易紀錄` 有表格。
   - `每月` 有月資料。
   - `每季` 有配息與 54C。
   - `持股` 有前十大持股與產業分布。
   - `每年` 有年度配息與稅務總覽。
   - `使用說明` 可正常顯示。

如果首頁仍一直轉圈，先看 Streamlit Cloud 的 `Manage app -> Logs`，找紅色錯誤訊息。

如果頁面能開，但按「更新資料」才失敗，通常是雲端執行 `fetch_data.py` 抓外部資料時被擋、超時，或外部網站暫時無回應；此時先保留上次 JSON 顯示，再針對抓取流程處理。

### 6.3 手機首頁版型規格

手機版只處理首頁，不更動桌機版完整分頁配置。2026-05-25 起手機首頁採獨立 DOM，不再用 CSS 壓縮桌機首頁。

手機斷點：

- 寬度 `760px` 以下啟用手機首頁版型。
- 桌機版仍保留左側導覽與完整分頁。

手機版顯示規則：

- 手機顯示 `section.mobile-home`，桌機首頁 DOM 在手機隱藏。
- 隱藏左側深色導覽列，避免佔掉手機第一屏。
- 隱藏非首頁分頁，包括交易紀錄、每月、每季、持股、每年、燈號設定與使用說明。
- 手機首頁包含五大區塊：
  - Hero Banner。
  - 核心數據與更新狀態。
  - 首頁重點總覽。
  - 走勢總覽。
  - 雲端更新按鈕列。
- 手機版目前不顯示底部導航列；底部應停在走勢總覽折溢價圖 X 軸日期下方一點。
- 核心數據與更新狀態整合原本九個核心欄位與資料更新狀態，不再顯示原本獨立的大型資料更新區塊。
- 手機首頁採金融 App 風格：白底、青綠到藍色漸層 Hero、圓形燈號、圓角卡片、柔和陰影、彩色 icon。
- 圖表使用手機專用尺寸重繪，不把桌機 `1120px` 圖表硬縮到手機寬度。
- 手機圖表目前使用獨立 `#mobileMainChart`，SVG viewBox 約 `390 x 226`，並減少日期標籤密度。
- Streamlit Cloud 內嵌元件會回報內容高度，避免手機出現內外兩層捲動區。
- 手機版 Streamlit 高度修正要同時處理兩層：`iframe.stIFrame` 與外層 `div[data-testid="stElementContainer"]`。外層容器若仍保留 `flex: 0 0 2600px`，即使 iframe 已縮小，手機圖表下方仍會出現大片留白。
- 目前手機版高度設定以 `1624px` 為基準：`iframe`、`stElementContainer height/max-height`、`stElementContainer flex-basis` 必須一致。
- 手機圖表關閉 hover 觸控層，避免手指上下滑動時被圖表互動區攔截。
- 手機版不得影響桌機版 `index.html` 的原始頁面配置。

手機首頁目前主要 class：

- `.mobile-home`
- `.mobile-home__hero`
- `.hero-banner`
- `.hero-status-orb`
- `.hero-meta`
- `.hero-actions`
- `.core-status-card`
- `.core-status-grid`
- `.core-metric-tile`
- `.metric-status-line`
- `.home-focus-card`
- `.mobile-focus-grid`
- `.trend-card`
- `.mobile-bottom-nav`
- `.ui-status-chip`

手機測試標準：

1. 在寬度約 `390px` 的手機畫面確認沒有水平捲動。
2. 首頁第一屏不應出現深色側邊欄。
3. 倉位資訊與重點總覽卡片應穩定排列，不因文字長短造成左右跳動。
4. 每日圖表需完整出現在走勢總覽卡內，線圖、折溢價線、成交量柱與日期軸都能辨識。
5. 上下滑動時不應卡在內嵌儀表板區域，也不應需要滑到右側 scrollbar 才能繼續。
6. 手機版只顯示首頁，桌機版仍可切換所有分頁。

### 6.4 常見雲端問題判斷

- 能開但不是原本 UI：可能上傳到的是原生 Streamlit 修正版，需改用 `ui_fixed.zip`。
- 一直轉圈：可能是舊版 iframe 讀 `static/index.html`，或 JS/CSS 被 Streamlit Cloud 靜態檔限制影響。
- GitHub Desktop 右邊顯示 `No content changes found`：只代表目前選到的單一檔案沒有差異；左側若有 changed files，仍可 commit。
- Streamlit Cloud 找不到 app 網址：登入 Streamlit Cloud dashboard，從 app 列表開啟，或從 `Manage app` 查看網址。
- GitHub 上看不到資料夾內容：確認不是把 ZIP 外層資料夾整包丟進 repo，repo 最上層必須直接有 `streamlit_app.py`。

## 7. 出版前檢查清單

每次要視為正式可用版本前，確認：

- 首頁總燈號可正常顯示綠 / 黃 / 紅。
- 總燈號有一句可讀原因。
- 資料更新狀態六格都有顯示。
- 資料完整性警示可偵測缺漏欄位。
- 交易紀錄新增、編輯、刪除、匯入、匯出正常。
- 每月含息報酬會隨交易紀錄增減變動。
- 每月即使無交易也會產生月度報酬點。
- 每季配息會依除息日持股計算。
- 持股頁能顯示前十大、產業與汰換。
- 每年頁能顯示年度配息、年度 54C 與補充保費觀察。
- 使用說明頁可從左側選單開啟。
- 雲端 UI 修正版與本機 `.bat` 版畫面一致或接近一致。
- Streamlit Cloud 可以讀取 repo 內 `data/*.json` 並正常顯示。
- 手機版前，首頁資訊順序必須再檢查一次。

## 8. 目前待辦

短期：

- 手機版首頁已暫時完成，除非發現 bug，先不要再調整手機首頁。
- 電腦版首頁第一版已完成；若要微調，優先檢查 `static/index.html`、`static/assets/styles.css`、`static/assets/app.js`，再同步根目錄 `index.html` / `assets/*`。
- 下一步將桌機新版 UI 標準延伸到每月、每季、持股、每年、燈號設定等分頁。
- 確認更新按鈕在新版 `dashboard_server.py` 下可穩定執行完整抓取。
- 把資料更新錯誤訊息做得更白話，例如「MoneyDJ 月資料抓不到」。

中期：

- 將手機版首頁建立的 UI 標準延伸到桌機版與其他分頁。
- 決定雲端資料儲存方式。
- 決定交易紀錄雲端同步方式。

長期：

- 雲端部署。
- 手機與電腦共用同一份資料。
- 報稅季用實際券商所得明細校正年度 54C。
